export interface IUser{
  firstName:string;
  lastName:string;
  email:string;
}

export interface ICandidate {
  _id: string;
  firstName: string,
  lastName: string,
  email: string,
  phone: string,
  dateOfBirth: string

}